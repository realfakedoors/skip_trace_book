<% if @user %>
  <% title = "#{@user.name}'s Groups" %>
<% else %>
  <% title = "All Groups" %>
<% end %>

<%= link_to 'New Group', new_group_path, class: "button is-primary" %>

<%= content_tag :hr, "", class: "hr" %>

<%= render 'shared/tiled_entities', entities: @groups, title: title %>