<% provide(:title, "#{@user.name}'s Friends") %>

<%= link_to "Friend Requests", received_friend_requests_path, class: "button is-primary" %>

<%= content_tag :hr, "", class: "hr" %>

<%= render 'shared/user_list', users: @friends %>